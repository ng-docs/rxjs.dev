import { DocTypeNamePipe } from './doc-type-name.pipe';

describe('DocTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DocTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
